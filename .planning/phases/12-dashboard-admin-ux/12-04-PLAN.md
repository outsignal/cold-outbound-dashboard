---
phase: 12-dashboard-admin-ux
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - src/app/(admin)/agent-runs/page.tsx
  - src/components/operations/agent-run-table.tsx
  - src/app/api/agent-runs/route.ts
autonomous: true
requirements: [DASH-07]

must_haves:
  truths:
    - "Admin can view all agent runs in a compact table with summary rows"
    - "Each row shows agent type, client/workspace, status, started at, and duration"
    - "Clicking a row expands an inline accordion showing full run details (input, output, steps, errors)"
    - "Table supports filtering by agent type, status, and workspace"
  artifacts:
    - path: "src/app/(admin)/agent-runs/page.tsx"
      provides: "Agent run monitoring page"
    - path: "src/components/operations/agent-run-table.tsx"
      provides: "Compact table with expandable accordion rows for agent run details"
    - path: "src/app/api/agent-runs/route.ts"
      provides: "API endpoint for paginated, filtered agent run listing"
  key_links:
    - from: "src/app/(admin)/agent-runs/page.tsx"
      to: "/api/agent-runs"
      via: "Client-side fetch with filter params"
      pattern: "fetch.*api/agent-runs"
    - from: "src/components/operations/agent-run-table.tsx"
      to: "AgentRun model"
      via: "Data display with JSON parsing for steps/input/output"
      pattern: "JSON\\.parse"
---

<objective>
Create an agent run monitoring page with compact Datadog/Grafana-style table density. Summary rows expand into inline accordions showing full run details — Vercel function logs style, no separate detail page.

Purpose: Admins need to monitor agent runs (leads, writer, campaign, research) across all workspaces — see what's running, what failed, and drill into errors without leaving the page.

Output: Agent run monitoring page at /agent-runs with filterable, expandable table.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-dashboard-admin-ux/12-CONTEXT.md

@prisma/schema.prisma

<interfaces>
<!-- AgentRun model -->
AgentRun fields:
- id: String
- agent: String  // "research" | "leads" | "writer" | "campaign"
- workspaceSlug: String?
- input: String  // JSON
- output: String? // JSON
- status: String  // "running" | "complete" | "failed"
- steps: String?  // JSON - tool call log
- durationMs: Int?
- error: String?
- triggeredBy: String? // "orchestrator" | "cli" | "api" | "pipeline"
- createdAt: DateTime

Existing UI components:
- Table, TableBody, TableCell, TableHead, TableHeader, TableRow from @/components/ui/table
- Badge from @/components/ui/badge
- Select from @/components/ui/select
- Button from @/components/ui/button
- Skeleton from @/components/ui/skeleton
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agent runs API endpoint</name>
  <files>
    src/app/api/agent-runs/route.ts
  </files>
  <action>
Create GET `/api/agent-runs` endpoint:
- Query params:
  - `agent` (optional): filter by agent type — "research", "leads", "writer", "campaign"
  - `status` (optional): filter by status — "running", "complete", "failed"
  - `workspace` (optional): filter by workspaceSlug
  - `page` (optional, default 1): pagination
  - `limit` (optional, default 50): page size, max 100
- Queries AgentRun with filters, ordered by createdAt desc.
- Returns:
  ```typescript
  {
    runs: AgentRun[],
    total: number,
    page: number,
    totalPages: number
  }
  ```
- Uses `prisma.agentRun.findMany` with where clause built dynamically from params.
- Uses `prisma.agentRun.count` for total.
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx tsc --noEmit src/app/api/agent-runs/route.ts 2>&1 | head -20</automated>
  </verify>
  <done>API endpoint returns paginated, filtered agent runs.</done>
</task>

<task type="auto">
  <name>Task 2: Agent run table component + monitoring page</name>
  <files>
    src/components/operations/agent-run-table.tsx
    src/app/(admin)/agent-runs/page.tsx
  </files>
  <action>
Create `src/components/operations/agent-run-table.tsx` ("use client"):
- Compact table density per CONTEXT.md — "Datadog/Grafana-style, power-user feel. Dense rows, lots of data visible at once."
- Columns: Agent Type (badge: color per agent), Workspace, Status (badge: green/yellow/red), Started At (relative time), Duration (formatted ms/s/m), Triggered By.
- Each row is clickable — clicking toggles an inline expanded section below the row (accordion style, not a separate page per CONTEXT.md: "Vercel function logs style — no separate detail page").
- Expanded section shows:
  - **Input**: Pretty-printed JSON (parsed from string) in a scrollable code block with monospace font.
  - **Output**: Pretty-printed JSON if exists, or "No output" in muted text.
  - **Steps**: If steps JSON exists, render as a list of tool calls (parse JSON array, show each step with name and truncated args).
  - **Error**: If error exists, show in a red-tinted box with monospace font.
- Only one row expanded at a time (clicking a new row closes the previous).
- Agent type badge colors: leads=blue, writer=purple, campaign=green, research=amber.
- Status badge: running=yellow (with pulse animation), complete=green, failed=red.
- Duration: show "—" for running (no duration yet), format as "1.2s", "45s", "2m 15s".
- Tailwind classes for compact density: `text-xs`, `py-1.5 px-2` on cells (not the default `py-4`).

Create `src/app/(admin)/agent-runs/page.tsx` ("use client"):
- Header: "Agent Runs" with filter controls inline.
- Filters row: Agent type Select (All, Research, Leads, Writer, Campaign), Status Select (All, Running, Complete, Failed), Workspace Select (All, + workspace list).
- Filters use `nuqs` useQueryState for URL persistence.
- Fetches from /api/agent-runs with filter + pagination params.
- Renders AgentRunTable with fetched data.
- Pagination at bottom: Previous/Next buttons with page info ("Page 1 of 12").
- Loading: skeleton rows (8 rows of skeleton cells).
- Empty state: "No agent runs found" with current filter context.
- Auto-refresh every 30 seconds (for monitoring running jobs) via `useEffect` with `setInterval`. Only auto-refreshes if any runs have status "running".
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Agent run monitoring page at /agent-runs with compact table, expandable inline details, filters by agent/status/workspace, pagination, and auto-refresh for running jobs.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes without errors
2. /agent-runs page shows a compact table of all agent runs
3. Clicking a row expands inline details showing input, output, steps, errors
4. Agent type, status, and workspace filters narrow the results
5. Pagination works (previous/next)
6. Running jobs show a pulse animation and the page auto-refreshes
7. Table density is compact — Datadog style, not the default shadcn spacing
</verification>

<success_criteria>
- Agent run monitoring page exists at /agent-runs
- Compact table density with expandable accordion rows
- Full run details (input, output, steps, errors) visible inline without navigation
- Filters by agent type, status, and workspace work correctly
- Auto-refresh for running jobs provides near-real-time monitoring
</success_criteria>

<output>
After completion, create `.planning/phases/12-dashboard-admin-ux/12-04-SUMMARY.md`
</output>
