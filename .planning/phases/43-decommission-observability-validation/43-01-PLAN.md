---
phase: 43-decommission-observability-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - trigger/sync-senders.ts
  - trigger/inbox-check.ts
  - trigger/postmaster-stats-sync.ts
  - trigger.config.ts
autonomous: true
requirements: [DECOMM-01]
must_haves:
  truths:
    - "Postmaster Stats Sync runs daily at 10am UTC as a Trigger.dev scheduled task"
    - "sync-senders logic runs as part of inbox-check (one fewer schedule slot used)"
    - "Any task failure sends a Slack alert to #outsignal-ops with task name, error, and run link"
    - "cron-job.org has zero active jobs (Postmaster Stats Sync disabled)"
  artifacts:
    - path: "trigger/postmaster-stats-sync.ts"
      provides: "Scheduled task replacing cron-job.org Postmaster Stats Sync"
      contains: "schedules.task"
    - path: "trigger/inbox-check.ts"
      provides: "Consolidated inbox-check with sync-senders logic"
      contains: "syncSenders"
    - path: "trigger.config.ts"
      provides: "Global onFailure hook for Slack alerting"
      contains: "onFailure"
  key_links:
    - from: "trigger.config.ts"
      to: "Slack API"
      via: "inline fetch in onFailure hook"
      pattern: "slack.com/api/chat.postMessage"
    - from: "trigger/postmaster-stats-sync.ts"
      to: "@/lib/postmaster/sync"
      via: "import"
      pattern: "syncPostmasterStats"
---

<objective>
Consolidate sync-senders into inbox-check to free a schedule slot, create a postmaster-stats-sync Trigger.dev task, add global onFailure Slack alerting, deploy, and disable the last cron-job.org job — achieving zero active external crons.

Purpose: Retire cron-job.org completely and ensure all task failures produce Slack alerts.
Output: Three modified/new trigger files, updated trigger.config.ts, zero active cron-job.org jobs.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-decommission-observability-validation/43-CONTEXT.md
@.planning/phases/43-decommission-observability-validation/43-RESEARCH.md
@.planning/phases/42-remaining-cron-lift-and-shift/42-05-SUMMARY.md

@trigger.config.ts
@trigger/sync-senders.ts
@trigger/inbox-check.ts
@src/app/api/cron/postmaster-sync/route.ts
@src/lib/postmaster/sync.ts
@src/lib/postmaster/alerts.ts
@src/lib/postmaster/client.ts

<interfaces>
From trigger/queues.ts:
- anthropicQueue, emailBisonQueue (shared concurrency queues)

From trigger.config.ts:
- defineConfig with prismaExtension, project ref, maxDuration: 300, dirs: ["./trigger"]

Current schedule inventory (10/10 slots):
- retry-classification: */30 * * * *
- snapshot-metrics: 0 0 * * *
- generate-insights: 0 */6 * * *
- sync-senders: 0 5 * * *
- inbox-check: 0 6 * * *
- invoice-processor: 0 7 * * *
- deliverability-digest: 0 8 * * 1
- domain-health: 0 8,20 * * *
- bounce-monitor: 0 */4 * * *
- poll-replies: */10 * * * *
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate sync-senders into inbox-check + create postmaster-stats-sync + add onFailure hook</name>
  <files>
    trigger/sync-senders.ts
    trigger/inbox-check.ts
    trigger/postmaster-stats-sync.ts
    trigger.config.ts
  </files>
  <action>
**Schedule consolidation — merge sync-senders into inbox-check:**

1. Read `trigger/sync-senders.ts` and extract the run function body (the sync logic).
2. In `trigger/inbox-check.ts`, import the same sync logic and call it at the END of the existing run function (after sender health checks). Add a console.log prefix: `[inbox-check] Running sync-senders...`.
3. In `trigger/sync-senders.ts`, REMOVE the `schedules.task()` wrapper. Keep ONLY the exported run logic as a plain async function (so inbox-check can call it). If sync-senders already exports a lib function, just import that in inbox-check. If not, refactor to export the logic as a function like `runSyncSenders()` and have inbox-check call it.
4. Verify inbox-check now does: inbox checks + sender health + sync senders, all in one scheduled run at `0 6 * * *`.

**Create postmaster-stats-sync task:**

1. Read `src/app/api/cron/postmaster-sync/route.ts` to understand the existing logic.
2. Create `trigger/postmaster-stats-sync.ts` as a `schedules.task()`:
   - id: `"postmaster-stats-sync"`
   - cron: `"0 10 * * *"` (daily 10am UTC, matching cron-job.org schedule)
   - maxDuration: 300
   - retry: `{ maxAttempts: 2, factor: 2, minTimeoutInMs: 5_000, maxTimeoutInMs: 60_000 }`
   - Run function: import and call `syncPostmasterStats()` from `@/lib/postmaster/sync` and `checkAndAlert()` from `@/lib/postmaster/alerts`. Check `isPostmasterConfigured()` first — skip if not configured. Follow the exact logic from the existing route.
   - Use PrismaClient at module scope for any DB queries needed (same pattern as other trigger tasks).

**Global onFailure hook in trigger.config.ts:**

1. Add `onFailure` to the `defineConfig()` call.
2. Use INLINE fetch to Slack API (do NOT import src/lib/slack — build-time import risk per research pitfall 2).
3. Implementation:
   ```
   onFailure: async ({ error, ctx }) => {
     const opsChannelId = process.env.OPS_SLACK_CHANNEL_ID;
     const slackToken = process.env.SLACK_BOT_TOKEN;
     if (!opsChannelId || !slackToken) return;

     const workspaceTag = ctx.run.tags?.find((t: string) => !t.startsWith("run_")) ?? "N/A";
     const errorMessage = error instanceof Error ? error.message : String(error);
     const runUrl = `https://cloud.trigger.dev/runs/${ctx.run.id}`;

     await fetch("https://slack.com/api/chat.postMessage", {
       method: "POST",
       headers: {
         Authorization: `Bearer ${slackToken}`,
         "Content-Type": "application/json",
       },
       body: JSON.stringify({
         channel: opsChannelId,
         text: `Task failed: ${ctx.task.id}`,
         blocks: [
           {
             type: "section",
             text: {
               type: "mrkdwn",
               text: `*Task failed:* \`${ctx.task.id}\`\n*Workspace:* ${workspaceTag}\n*Error:* ${errorMessage}\n*Run:* <${runUrl}|View in Trigger.dev>`,
             },
           },
           {
             type: "context",
             elements: [
               {
                 type: "mrkdwn",
                 text: `Run ID: \`${ctx.run.id}\` | Attempt: ${ctx.attempt.number} | ${new Date().toISOString()}`,
               },
             ],
           },
         ],
       }),
     }).catch((err) => console.error("[onFailure] Slack alert failed:", err));
   }
   ```
4. Keep all existing config (project, maxDuration, dirs, build.extensions) unchanged.
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - trigger/inbox-check.ts includes sync-senders logic at the end of its run function
    - trigger/sync-senders.ts no longer has a schedules.task() (just exports a function)
    - trigger/postmaster-stats-sync.ts exists as a schedules.task with cron "0 10 * * *"
    - trigger.config.ts has a global onFailure hook using inline Slack fetch
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Trigger.dev Cloud + disable cron-job.org Postmaster Stats Sync</name>
  <files>
    trigger/sync-senders.ts
    trigger/postmaster-stats-sync.ts
  </files>
  <action>
**IMPORTANT: Two-step deploy to avoid schedule limit exceeded error (Pitfall 5 from research).**

**Step 1 — Deploy consolidation first (removes sync-senders schedule, goes from 10/10 to 9/10):**

1. Run `npx trigger.dev deploy` to deploy the consolidated code.
2. The sync-senders schedule should be automatically removed since the file no longer has `schedules.task()`.
3. After deploy, verify schedule count via Trigger.dev REST API:
   ```
   curl -s -H "Authorization: Bearer $TRIGGER_SECRET_KEY" "https://api.trigger.dev/api/v1/schedules" | node -e "process.stdin.on('data',d=>{const j=JSON.parse(d);console.log('Active schedules:',j.data.filter(s=>s.active).length)})"
   ```
4. Confirm 9/10 active schedules (sync-senders removed, postmaster-stats-sync not yet registered if it needs a second deploy).

If postmaster-stats-sync is already registered in this same deploy (since the file exists), verify 10/10 with the new task replacing sync-senders.

**Step 2 — Verify postmaster-stats-sync is registered:**

1. Check the Trigger.dev REST API for the postmaster-stats-sync schedule:
   ```
   curl -s -H "Authorization: Bearer $TRIGGER_SECRET_KEY" "https://api.trigger.dev/api/v1/schedules" | node -e "process.stdin.on('data',d=>{const j=JSON.parse(d);const ps=j.data.find(s=>s.task==='postmaster-stats-sync');console.log('Found:',!!ps,'Active:',ps?.active,'Cron:',ps?.generator?.expression)})"
   ```
2. Confirm: active=true, cron="0 10 * * *".

**Step 3 — Disable cron-job.org Postmaster Stats Sync (job ID 7368027):**

1. Use cron-job.org REST API to disable:
   ```
   curl -s -X PATCH "https://api.cron-job.org/jobs/7368027" \
     -H "Authorization: Bearer $CRONJOB_API_KEY" \
     -H "Content-Type: application/json" \
     -d '{"job":{"enabled":false}}'
   ```
2. Verify disabled:
   ```
   curl -s -H "Authorization: Bearer $CRONJOB_API_KEY" "https://api.cron-job.org/jobs/7368027" | node -e "process.stdin.on('data',d=>{const j=JSON.parse(d);console.log('Enabled:',j.jobDetails.enabled)})"
   ```

**Step 4 — Verify zero active cron-job.org jobs:**

```
curl -s -H "Authorization: Bearer $CRONJOB_API_KEY" "https://api.cron-job.org/jobs" | node -e "process.stdin.on('data',d=>{const j=JSON.parse(d);const active=j.jobs.filter(j=>j.enabled);console.log('Active jobs:',active.length);active.forEach(j=>console.log('-',j.title))})"
```

Expected: Active jobs: 0
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && curl -s -H "Authorization: Bearer $TRIGGER_SECRET_KEY" "https://api.trigger.dev/api/v1/schedules" | node -e "process.stdin.on('data',d=>{const j=JSON.parse(d);const active=j.data.filter(s=>s.active);console.log('Schedules:',active.length+'/10');active.forEach(s=>console.log(' ',s.task,s.generator?.expression))})"</automated>
  </verify>
  <done>
    - Trigger.dev Cloud shows 10/10 active schedules (sync-senders replaced by postmaster-stats-sync)
    - postmaster-stats-sync schedule active at "0 10 * * *"
    - cron-job.org has zero active jobs (all 8 jobs disabled including Postmaster Stats Sync #7368027)
    - No schedule limit exceeded errors during deploy
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Trigger.dev dashboard shows postmaster-stats-sync scheduled at 10am UTC daily
- Trigger.dev dashboard shows inbox-check still runs at 6am UTC daily (now includes sync-senders logic)
- cron-job.org account shows 0 active jobs
- trigger.config.ts contains onFailure hook
</verification>

<success_criteria>
- Zero active cron-job.org jobs
- 10/10 Trigger.dev schedule slots used (sync-senders replaced by postmaster-stats-sync)
- Global onFailure hook sends Slack alerts to #outsignal-ops on task failure
</success_criteria>

<output>
After completion, create `.planning/phases/43-decommission-observability-validation/43-01-SUMMARY.md`
</output>
